# Java-DAO-Funcionario-Dependente
Aula prática Java — CRUD de funcionários e dependentes com padrão DAO, conexão MySQL via JDBC e interface via JOptionPane, incluindo listagem com JOIN entre as duas tabelas.
